/*
Problem 3) The body of the following method has three statements. Rewrite the body so that it 
has just a single statement which is the simplest statement that will work.
   private static boolean isInside(int n)
   {
      boolean inside = false;
      if ( 0 < n && n < 100) inside = true;
      return inside;
   }
   All you need to put is below
    return ( 0 < n && n < 100);
*/
